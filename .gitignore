# Local git worktrees (see AGENTS.md)
.worktrees/

# Python
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.venv/
venv/
# Symlink form used by `make setup-worktree` in linked worktrees (the
# trailing-slash entries above only match real directories).
backend/.venv
backend/db.sqlite3

# Node
node_modules/
# Symlink form used by `make setup-worktree` in linked worktrees.
frontend/node_modules
e2e/node_modules
frontend/dist/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Playwright
playwright-report/
test-results/
blob-report/

# Local env (never commit secrets)
.env
.env.local
.env.*.local

# OS / editor
.DS_Store

# Claude Code settings — personal / ephemeral; do not check in.
.claude/
